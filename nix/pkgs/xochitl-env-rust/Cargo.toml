[package]
name = "xochitl-env"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", default-features = false, features = ["mount", "sched", "fs", "user", "resource"] }

[profile.release]
strip = true
